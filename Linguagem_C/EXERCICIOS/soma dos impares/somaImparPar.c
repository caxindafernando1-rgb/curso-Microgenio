#include <stdio.h>
#include <locale.h>
#include <windows.h>  

int main() {
    
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);
    setlocale(LC_ALL, ".UTF8");
      int  s, d, e, f;
    
    for( d = 0; d )
    s = (1 + 999) * 500;
    d = s / 2;
            printf("A soma dos impares entre 0 a 1000:\n%d", d);

      

}